[package]
name = "bevy-storyengine"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
bevy = "0.10.1"
bevy_transform_gizmo = "0.7"
bevy_mod_picking = { version = "0.13", features=["backend_raycast"]}
bevy-inspector-egui = {version = "0.18.3", features=["highlight_changes"]}

#[profile.release]
#opt-level = "z"
#lto = "fat"
