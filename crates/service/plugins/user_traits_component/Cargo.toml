[package]
publish = false
name = "user_traits_component"
edition = "2018"
version = "0.0.1"
authors = [
    "Brandon Thomas <bt@brand.io>",
    "Brandon Thomas <echelon@gmail.com>",
]

[lib]
name = "user_traits_component"
path = "src/lib.rs"

[dependencies]

# Internal
errors = { path = "../../../lib/errors" }

# Workspace hack (faster compile times)
my-workspace-hack = { version = "0.1", path = "../../../my-workspace-hack" }

# Web
actix-web = "4.4.0"

[dev-dependencies]
mockall = "0.11.3"
