# For some reason, we can't pass json files to the tauri dev command
[build]
frontendDist = "../../../frontend/apps/editor2d/dist"
beforeDevCommand = "npx nx run editor2d:dev"
devUrl = "http://localhost:5741"
