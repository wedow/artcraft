// import { VideoAudioPreProcessor } from "app/pages/PageEnigma/Editor/VideoProcessor/video_audio_preprocessor.ts";

// const processor = new VideoAudioPreProcessor();

// // chrome://inspect/#workers debug shared worker.
// self.addEventListener("connect", function (event: MessageEvent) {
//   const port = (event as MessageEvent & { ports: MessagePort[] }).ports[0];

//   port.addEventListener("message", function (event: MessageEvent) {
//     console.log("Message received from main script:", event.data);
//     port.postMessage("Hello from the Shared Worker!");
//   });

//   port.start();
// });
