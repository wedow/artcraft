import { signal } from "@preact/signals-react";

export const assetModalVisibleDuringDrag = signal(true);
export const reopenAfterDragSignal = signal(false);
export const assetModalVisible = signal(false);
