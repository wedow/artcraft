import { Colors } from "../constants";

export enum NodeType {
  IMAGE = "image",
  TEXT = "text",
  VIDEO = "video",
}
export const transparent = Colors.transparent;
export const primaryOrange = Colors.primaryOrange;

export const highlightStrokeWidth = 10;
export const minNodeSize = {
  width: 200,
  height: 200,
};
