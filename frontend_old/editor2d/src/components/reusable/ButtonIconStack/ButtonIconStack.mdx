import { Canvas, Meta } from '@storybook/blocks';

import * as ButtonIconStackStories from './ButtonIconStack.stories';

<Meta of={ButtonIconStackStories} />

# ButtonIconStackStories

A button denotes a possible action to be taken on behalf of the user.

This button shows only a single icon.

<Canvas of={ButtonIconStackStories.Default} />
