+++
+++

# the MAIN INDEX

1. This page should never be seen by any user, all deployed contents are in their separate directories.
2. All `_index.md` files are the blog-roll page for each directory, except for this page, this is the directory of the root of `context/`
3. In addition, although this is technically a **blog-roll**, it does not use `section.html` as its template like other direcotry's blog-roll; by default, this page only loads `index.html` as its template.
4. a blog-roll consist of some example pages should be shown for the deployment of this page.
