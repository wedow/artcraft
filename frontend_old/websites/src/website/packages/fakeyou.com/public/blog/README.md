# BLOG for StoryTeller AI

This folder is important for deployment of our blog.

However, it is managed in another package in this repo, named `gatsby-blog`.

## Update Blog

1. Go into `gatsby-blog`
2. run `gatsby build`
3. move the content in `gatsby-blog`'s `public` directory into this directory.