import { Canvas, Meta } from '@storybook/blocks';

import * as PillStories from './Pill.stories';

<Meta of={PillStories} />

# Pill

A pill is a container around the children.

<Canvas of={PillStories.Default} />
