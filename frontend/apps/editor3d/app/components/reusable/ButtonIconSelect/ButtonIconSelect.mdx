import { Canvas, Meta } from '@storybook/blocks';

import * as ButtonIconSelectStories from './ButtonIconSelect.stories';

<Meta of={ButtonIconSelectStories} />

# ButtonIconSelectStories

A button denotes a possible action to be taken on behalf of the user.

This button shows only a single icon.

<Canvas of={ButtonIconSelectStories.Default} />
