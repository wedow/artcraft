import { Canvas, Meta } from '@storybook/blocks';

import * as ButtonIconStories from './ButtonIcon.stories';

<Meta of={ButtonIconStories} />

# ButtonIcon

A button denotes a possible action to be taken on behalf of the user.

This button shows only a single icon.

<Canvas of={ButtonIconStories.Default} />
