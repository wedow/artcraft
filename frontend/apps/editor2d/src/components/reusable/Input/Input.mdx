import { Canvas, Meta } from '@storybook/blocks';

import * as InputStories from './Input.stories';

<Meta of={InputStories} />

# Input control

Here is the input with placeholder
<Canvas of={InputStories.Default} />

Here is the input with icon
<Canvas of={InputStories.WithIcon} />

Here is the input with label
<Canvas of={InputStories.WithLabel} />

Here is the input with error message
<Canvas of={InputStories.WithError} />
