import { Canvas, Meta } from '@storybook/blocks';

import * as ButtonLinkStories from './ButtonLink.stories';

<Meta of={ButtonLinkStories} />

# ButtonLink

A button denotes a possible action to be taken on behalf of the user.

The button label comes via "children".

There are three types of Button: primary, secondary, action.

<Canvas of={ButtonLinkStories.Primary} />
<Canvas of={ButtonLinkStories.Secondary} />

It can be shown with an optional icon.

<Canvas of={ButtonLinkStories.WithIcon} />
