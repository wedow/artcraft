import { Canvas, Meta } from '@storybook/blocks';

import * as InputVectorStories from './InputVector.stories';

<Meta of={InputVectorStories} />

# 3 Input controls: one for x, y, and z
It allows entry os plus or minus numbers with decimals. It can also be adjusted via dragging the mouse left or right.

<Canvas of={InputVectorStories.Default} />
