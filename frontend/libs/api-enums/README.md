# api-enums

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build api-enums` to build the library.

## Running unit tests

Run `nx test api-enums` to execute the unit tests via [Vitest](https://vitest.dev/).
